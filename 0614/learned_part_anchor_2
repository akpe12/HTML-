anchor tag는 여러 방식으로 쓰일 수가 있다.
✔ href attribute에 바로 주소값을 넣어서 이동시킬 수 있다.

✔ href attribute에 주소값을 저장한 파일의 상대경로를 넣어줘서 이동시킬 수도 있다.

✔ id 값을 anchor 태그 안에 적어주어서 페이지 내 이동을 간편하게 해줄 수도 있다.
-> <a href="#custom">custom문단으로 이동</a>
-> <a id="custom">이번에는 custom을 ~</a>

✔ href attribute에 mailto:메일 주소를 넣어주어서 클릭 시 바로 메일 쓰기로 이동하도록 만들어줄 수도 있다.

✔ href attribute에 tel:전화번호를 넣어주어서 클릭 시 바로 전화 연결을 하도록 만들어줄 수도 있다.

🎈href의 값 뒤에 target="_blank"를 적어주면 새로운 탭을 만들어주고 그 탭에서 href 값으로 이동시켜준다.
🎈따라서 target="_blank"를 적어주지 않으면, 현재 탭에서 다른 페이지로 이동하라고 명령하는 것이다.
